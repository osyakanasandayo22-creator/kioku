# Vercel の Environment Variables に同じ名前で設定してください（クライアントに公開しない）。
GEMINI_API_KEY=
# 互換用（どちらか一方で可）
GOOGLE_API_KEY=

# 任意。未設定時は api/grade.js 内の既定モデルを使用します。
GEMINI_MODEL=gemini-3.1-flash-lite-preview
# 任意。未設定時は v1（必要なら v1beta を指定）
GEMINI_API_VERSION=v1
